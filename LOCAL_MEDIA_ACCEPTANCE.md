# 本地素材验收记录

日期：2026-09-06。平台：macOS，本地 SenseVoice 模型。

## 范围

从用户指定目录选取一个原始 TS 视频（H.264/AAC，约 7904 秒），直接测试原文件，未转封装。视频画面隐藏、播放静音。原视频、SRT 和已有项目未修改；报告不包含媒体画面、识别文本及个人文件路径。

## 播放定位

| 操作 | 目标 | 后端实际位置 | 确认耗时 |
| --- | --- | --- | --- |
| 暂停时跳转 | 60 秒 | 60 秒 | 125 ms |
| 暂停时跳转 | 600 秒 | 600 秒 | 54 ms |
| 暂停时跳转 | 30 秒 | 30 秒 | 52 ms |
| 连续前进四次、后退两次 | 50 秒 | 50 秒 | — |
| 静音播放中前进两次、后退一次 | 60.063 秒 | 60.104 秒 | — |

每次前进/后退 10 秒。播放中跳转后仍保持播放状态，实际位置相差约 41 ms（包含正常播放推进）。本项调用真实播放器控制器，验证原生跳转与位置确认；键盘事件路由另由现有组件测试覆盖，不代表人工观看后的主观流畅度评价。

## 选区识别

同一 24–45 秒选区，保留原始音量。小声预设使用 +6 dB、VAD 阈值 0.35、句末静音 0.5 秒、最短语音 0.15 秒。

| 配置 | 输出语言标签 | 字幕段数 | 接收的语音时长 | 耗时 |
| --- | --- | --- | --- | --- |
| 自动检测，0 dB | ja、yue | 8 | 12.798 秒 | 4.770 秒 |
| 指定日语，0 dB | ja | 8 | 12.798 秒 | 4.564 秒 |
| 指定日语＋小声预设 | ja | 3 | 15.770 秒 | 4.607 秒 |

指定日语消除了本选区中混合的语言标签。小声预设改变了检测范围及分句，不能据此认定准确率提高或遗漏已全部消除；尚无人工标注基准，未计算字错率。此处的语音时长是识别段时长之和，并非人工测得的有效说话时长。

独立测试字幕验证了：选区超出旧字幕结果时长时可以识别；预览不修改当前结果；确认替换保留区外字幕；撤销恢复旧字幕。未写回用户已有项目。

## 验证结果与复现

两项真实素材集成测试通过，静态分析无问题。新增脚本见 `app/integration_test/local_media_acceptance_test.dart`，复现命令见 `app/README.md` 的“本地素材验收”。

此次只抽测一个文件和一个语音选区，尚未覆盖目录内全部视频、整片识别质量及各类低声/噪声场景。

## 2026-09-12 短视频补充 smoke

原 `Downloads/vv` 目录已不存在，本次改用 `Downloads/动画示范` 中两个 5–8 秒 H.264/AAC MP4，只验证真实原生管线，不作为长视频或语音质量结论。首个视频音轨解码耗时 141 ms、播放器打开耗时 142 ms；第二个播放列表条目打开耗时 118 ms。播放器阶段采样峰值 RSS 为 771,964,928 bytes。模型完整性、模型自带 WAV 识别、两个视频的音轨解码、播放推进和播放列表切换均通过。

文件夹页面新增回归，确认处理开始后可以返回主界面，控制器继续完成任务并写出字幕。任务中心、设置和长视频持续识别/翻译仍需有语音长视频时进行人工操作验收。

## 2026-09-13 短视频转写补充验收

使用 Downloads 中新增的 10 秒 H.264/AAC MP4 和原有两个 5–8 秒 MP4，再次运行 macOS 设备验收。10 秒视频音轨解码耗时 114 ms、播放器打开耗时 130 ms；另外两个播放列表条目打开耗时分别为 121 ms 和 112 ms。视频流式转写完整处理 159,994 个采样，队列等待 0 ms，总耗时 2,876 ms，其中模型准备 2,817 ms、实际识别 34 ms，峰值 RSS 为 768,376,832 bytes。素材没有识别到语音段，结果时长为 9.999625 秒，因此本次只证明无语音视频能够正常收尾，不作为识别准确率结论。

`device_acceptance_test.dart` 共 4 项通过，真实麦克风项因未配置而跳过，报告保存于仓库外的 `/private/tmp/vsasr_device_acceptance_20260913.json`。尝试自动操作已安装应用时，macOS 屏幕捕捉接口返回 `SCStreamErrorDomain -3811`，未能完成任务中心、设置和播放器之间的人工切换；长语音视频压力与界面操作验收继续保留。

为补充长任务技术验证，使用模型自带的 5.148 秒粤语测试 WAV 循环生成仓库外的 10 分钟 H.264/AAC 临时视频。播放器打开耗时 123 ms，600 秒音轨解码耗时 251 ms。流式转写处理 9,600,000 个采样、59 个分块并输出 117 个字幕段，首条定稿耗时 3,345 ms，总耗时 33,400 ms，其中模型准备 2,796 ms、识别计算 30,424 ms，队列等待 0 ms，转写阶段峰值 RSS 为 811,155,456 bytes。任务完整处理到 600 秒并正常收尾；验收报告位于 `/private/tmp/vsasr_device_acceptance_10min_20260913.json`。

这份循环素材可验证长时解码、分块转写、增量定稿和资源收尾，但语音内容高度重复，也没有在转写同时完成人工页面切换，因此不能作为自然语音准确率、长期内存趋势或界面并发操作的最终结论。

界面并发另增加组合组件回归：识别任务保持在解码阶段时，依次打开设置页、返回并切换视频播放页、打开任务中心查看任务，最后从任务中心取消。每次导航后控制器仍保持忙碌，取消后迟到的解码结果不会恢复旧任务。该回归验证页面生命周期和操作入口，仍不替代真实长视频运行期间的人工流畅度观察。

## VAD 分段校时评估

使用同一份 10 分钟循环语音视频分别执行完整转写和独立 Silero VAD 检测，两边均得到 117 个分段。将转写字幕统一后移 0.75 秒后交给保守校时建议器，平均边界误差由 0.75 秒降至 0，结果保持顺序且没有重叠。macOS 集成验收耗时约 42 秒并通过。

当前建议器要求字幕段与 VAD 段严格一一对应，任一边界调整超过阈值、段数不一致、区间越界或对齐后重叠都会拒绝生成结果。随后从本机既有 H.264/AAC 视频匿名截取 60 秒自然语音片段，完整转写与独立 VAD 均得到 14 段；注入 0.75 秒错位后，平均边界误差同样由 0.75 秒降至 0，macOS 集成验收耗时 11 秒并通过。验收只记录时长、段数和误差，不保存文件名、路径或字幕文本。

自然语音样本仍未覆盖段数不一致、漏检和多检，因此本功能暂不加入用户界面；验收脚本见 `app/integration_test/subtitle_alignment_acceptance_test.dart`。脚本会在 macOS Runner 中显示模型加载、识别和语音边界检测阶段，避免原生测试窗口无页面时呈现纯黑屏。

## 波形窗口补充验收

同一原始 TS 的 24–45 秒窗口，提取 600 个峰值点，实际处理 21 秒音频，耗时 187 ms。真实原生解码测试通过。测试只保存指标，不输出原始音频或字幕文本。波形界面另通过 390×844 与 1100×700 布局检查及中文字体截图检查；拖选、边界重叠拒绝、撤销、循环和切换窗口后的取消由组件测试覆盖。
